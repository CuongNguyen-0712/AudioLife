<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:VinhKhanhAudioGuide.Mobile.ViewModels"
             x:Class="VinhKhanhAudioGuide.Mobile.Views.MapPage"
             x:DataType="viewmodels:MapViewModel"
             Title="Bản đồ">

    <Grid RowDefinitions="*,Auto">

        <!-- Interactive Leaflet Map via WebView -->
        <WebView Grid.Row="0"
                 x:Name="MapWebView"
                 Source="{Binding MapHtmlSource}"/>

        <!-- Bottom Sheet - Nearby Locations -->
        <Frame Grid.Row="1"
               CornerRadius="20"
               Padding="0"
               Margin="0,-20,0,0"
               BackgroundColor="{StaticResource White}">
            <VerticalStackLayout Padding="15,12">
                <!-- Drag Handle -->
                <BoxView HeightRequest="4"
                         WidthRequest="40"
                         CornerRadius="2"
                         BackgroundColor="{StaticResource Gray300}"
                         HorizontalOptions="Center"
                         Margin="0,0,0,10"/>

                <!--Địa điểm gần đây-->
                <Label Text="Địa điểm gần đây"
                       FontSize="18"
                       FontAttributes="Bold"/>

                <CollectionView ItemsSource="{Binding NearbyLocations}"
                                HeightRequest="160"
                                ItemsLayout="HorizontalList">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="viewmodels:NearbyLocation">
                            <Frame Padding="0"
                                   Margin="5,8"
                                   CornerRadius="12"
                                   WidthRequest="140"
                                   HasShadow="True"
                                   IsClippedToBounds="True">
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MapViewModel}}, Path=LocationTappedCommand}"
                                        CommandParameter="{Binding .}"/>
                                </Frame.GestureRecognizers>
                                <Grid RowDefinitions="80,*">
                                    <BoxView BackgroundColor="{StaticResource Primary}"
                                             Opacity="0.6"/>
                                    <VerticalStackLayout Grid.Row="1"
                                                         Padding="8,6">
                                        <Label Text="{Binding Name}"
                                               FontSize="12"
                                               FontAttributes="Bold"
                                               LineBreakMode="TailTruncation"/>
                                        <Label Text="{Binding Distance, StringFormat='{0:F1} km'}"
                                               FontSize="10"
                                               TextColor="{StaticResource Primary}"/>
                                    </VerticalStackLayout>
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </Frame>
    </Grid>

</ContentPage>
